# The prop pulled in to the edge of its own hex.
#
# THE QUESTION. Whether a turret, a font and a weirwood read as "beside" the
# tower when they stand inside the tower's own tile rather than on the next one.
# Issue #282, question 4.
#
# WHAT TO LOOK AT. A hex's inradius is 1.0 metre, so 0.8 sideways is the prop
# standing just inside the edge, and nothing it does can reach a neighbour
# whatever is standing there. What it costs is the body: at 0.8 the prop is
# 0.2 from the tower's own feet, and whether the two read as one clump is the
# eye check. The shipped socket is 2.0, one column pitch.
#
# THIS IS A CHANGE TO NextTile AND NOTHING ELSE. Three rows, one number.

label     the prop tucked inside its own hex
question  whether a prop inside the tower's own tile still reads as beside it

stand  37  0.8,0
stand  25  0.8,0
stand  30  0.8,0
