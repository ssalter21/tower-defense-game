# A CANDIDATE LOOK, AND NOT CONTENT. Nothing loads this except
# tools/capture-match-frames.ps1 -Effects, and nothing about the game is decided
# here: client/Assets/View/MatchTuning.cs is what the game draws its effects at,
# and every value in it that this file names is declared a placeholder in that
# file's own header. This is one of the alternatives, so that signing one is
# somebody looking at pictures rather than reading constants.
#
# A KEY IS A MatchTuning MEMBER NAME and an unknown one is a fault that lists
# the near misses -- View.Editor.EffectLookFile. A `signature <unit id> <shape>`
# line moves which shape a row's bubble leaves, which is bound per unit and so
# cannot be asked with a number at all.
#
question A hastened body wears the same blue wash a slowed one does, and the ring over its head is the only thing that tells them apart -- and #270 proved the ring reads at 1x while the wash does not. Every frame in this group has a slowed creep and a hastened one on it at once.
label the slow deepened as well, so neither is the pale wash

SpeedEffectTint 0.28,0.5,0.95
HasteEffectTint 1,0.85,0.4
