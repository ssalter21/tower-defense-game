# The Artificer standing beside both the things his look names.
#
# THE QUESTION. docs/roster.md's tier-2 Engineer line wants a turret AND an
# ammo_crate; BesideProp holds one model, and the turret wins because it is
# where the shell leaves from -- so that rung is told from the Engineer by
# colour alone, which is thinner than the rule the same page sets for a tier 2.
# Issue #281.
#
# A SECOND SOCKET AND A COMBINED PROP ARE THE SAME PICTURE. They differ in how
# they would be BUILT -- a second socket on every tower in the game, or one
# authored model that is both -- and not in what the camera sees. So this is
# one candidate rather than two, and which mechanism a signed one becomes is
# the sitting's. The '+' parents both under one throwaway object at capture
# time and hands that to the one socket the game has; nothing in BesideProp,
# UnitArt, TowerView, MatchArt.asset or the generated scene moves. See
# BesideGroup.cs.
#
# THE OFFSET IS A STARTING LAYOUT AND NOT A SIGNATURE. The turret is 1.00 x
# 1.13 on the ground and the crate 0.57 x 0.49, on a 2.0 m tile, so 0.65
# sideways clears the turret's footprint by 0.15 m and keeps the crate inside
# the tile. AGENTS.md rule 6 puts a layout on the human side of the line: edit
# this line and run the capture again, no agent and no recompile in between.
#
# THE ANCHOR DOES NOT MOVE HERE, and that is the point of this candidate -- the
# turret stays where the shell leaves from, which is the whole reason the crate
# lost the socket in the first place.

label     turret and crate on the one tile
question  whether the Artificer can stand beside both the things his look names

beside  36  Kaykit/adventurers/turret_base.fbx*1~0,0+Kaykit/adventurers/ammo_crate.fbx*1~0.65,0.35
