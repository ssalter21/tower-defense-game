# The same three rungs, with the two steps spread as far as the tile allows.
#
# WHY THIS EXISTS. engineer-line-scaled.txt draws the reading Sam's words most
# likely carry -- 1.00, 1.25, 1.50 -- and the tier-2 step in it moves 225 px of
# 1,440,000. That is the same figure the Bishop's anchor moved, which is the
# change this map has been calling invisible. A rung that is signalled by an
# invisible step is not signalled.
#
# So this is the other end of the same idea: keep the signal, spend the whole
# bracket on it. 1.00, 1.50, 2.00 puts one full step between each rung and puts
# tier 3 exactly at the size where turret_base fills its tile.
#
# IT COSTS THE CEILING. *2.00 is where the prop stops fitting beside the tower
# and starts touching the corridor -- which is only a cost while a beside prop
# shares the tower's tile. Sam has also said props should stand on their own
# hex, and on its own hex the ceiling is a different number.
#
# NOTHING THE GAME SHIPS FROM MOVES. Applied to a copy of the wired art for the
# length of one run. See UnitArtFile.cs.

label     the Engineer line at 1.00, 1.50 and 2.00
question  does a full step between rungs make the tier-2 turret read at 1600x900

beside  36  Kaykit/adventurers/turret_base.fbx*1.50
beside  37  Kaykit/adventurers/turret_base.fbx*2.00
