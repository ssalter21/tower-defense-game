# Where a standing count of capstone tokens lives. Issue #282, question 3.
#
# THE QUESTION. A run is granted a capstone token at rounds 3, 6 and 9 and can
# spend it on nothing but the top of a line. Nothing on screen says how many
# are held, so a player holding none clicks a Bishop, sees a ladder that opens
# on nothing, and has nothing telling them why. The count is DERIVED FROM THE
# COMMAND STREAM AND NOT STORED (ADR-0062): Run.CapstoneTokens is what is
# granted through this round less what earlier rounds spent, so this is a
# display question with no state behind it.
#
# TWO STATES, because the counter has to say something in both:
#
#   wave 9   three granted, some spent by the rounds before, at least one held
#            -- the Bishop's ladder opens and offers the Consecration for it
#   wave 2   nothing granted yet -- the Bishop's ladder opens on nothing,
#            which is the moment the ticket names
#
# THE CANDIDATES, each a class in client/Assets/Editor/ChromeCandidates/:
#
#   tokens-w9-in-the-header.png     TokensInTheHeader: a fourth field after
#   tokens-w2-in-the-header.png     the gold, "Capstone tokens  N held  ·
#                                   N spent  ·  N to come"
#   tokens-w9-as-pips.png           TokensAsPipsInTheHeader: the word and
#   tokens-w2-as-pips.png           three pips, one per grant on the schedule
#                                   -- filled is held, dim is spent, hollow is
#                                   still to come
#   tokens-w9-at-the-ladder.png     TokensAtTheLadder: with the ladder open,
#   tokens-w2-at-the-ladder.png     a line at its top saying how many are
#                                   held; with the ladder shut on a tower
#                                   whose only rung is a capstone, a line at
#                                   the hex naming the rung and why it is not
#                                   offered ("needs a capstone token · next at
#                                   wave 3")
#   tokens-w2-as-built.png          wave 2 as it ships: the Bishop standing
#                                   and no ladder, for reading the three above
#                                   against
#
# THE WORDS ON THESE ARE PLACEHOLDERS. What a token is called on screen is a
# name a player reads, and an agent does not name things on this project;
# "capstone token" is RosterNames' existing wording and the rest is the
# minimum that makes the count legible. Sam spells it.
#
# NOT MOCKED, AND WHY: a count on the palette bar or on the wave row. Both
# bars already overflow at this roster -- the palette clips its ninth entry
# (#275) and the wave row is question 1 -- so a tag at the end of either is
# off the right edge of a 1600x900 window, and a sheet of it would be a
# picture of nothing.
#
# NOTHING HERE DECIDES ANYTHING. Signing one is #285's.
