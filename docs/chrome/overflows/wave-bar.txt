# The wave bar with every creep the roster has in it. Issue #282, question 1.
#
# THE QUESTION. Seventeen filled boxes and the trailing empty one want 3280
# panel units of the 1872 between the margins (a box is 176 wide with a 12
# gap; the panel lays out at 1920 whatever the window is, so at 1600x900 a
# unit is five sixths of a pixel). WaveBar.cs says scrolling or wrapping is
# the answer "when a played round actually overflows, and not before", and
# #270 played that round: ten boxes fit, the eleventh was cut in half, and
# the box a creep is added through was past 3000 pixels.
#
# THE STATE. Wave 9 of a run played forward from the opening round by the
# simulation's own scripted player on the tower side and by sending one of
# every creep the purse could still cover, cheapest first, on the wave side
# -- see UiPreviewCapture.PlayTo. A creep is bought once and attacks every
# round after, so by wave 9 the bar carries the whole roster. Every number on
# the sheet is the run's own.
#
# THE CANDIDATES, each a class in client/Assets/Editor/ChromeCandidates/ that
# rearranges the boxes the bar built and changes nothing a box says:
#
#   wave-bar-as-built.png          the bar as it ships -- the baseline every
#                                  candidate is read against
#   wave-bar-scrolls.png           WaveBarScrolls: the boxes in a horizontal
#                                  ScrollView, the row the height it is; the
#                                  option the toolkit gives for free
#   wave-bar-wraps.png             WaveBarWraps: flex-wrap onto a second line,
#                                  the row growing upward into the board
#   wave-bar-shrinks.png           WaveBarShrinks: 128-unit boxes, an 8 gap,
#                                  type at 18 -- thirteen of seventeen fit
#   wave-bar-shrinks-to-fit.png    WaveBarShrinksToFit: 96-unit boxes, type
#                                  at 13 -- the width at which seventeen fit
#                                  (17 x 104 + 60 = 1828 of 1872)
#
# WHAT TO LOOK AT. Whether a name at 13 points in a 96-unit box is a name at
# 1600x900; how much board two lines cost; whether a scroller hides the one
# box a creep is added through. The 1x frame is the one that decides.
#
# NOT MOCKED, AND WHY: a portrait rail down one side. That is the chosen
# arrangement docs/chrome/README.md already records from 17 August 2026
# ("the wave along a side rail in portraits") and it waits on
# RosterThumbnails, which answers with nothing until somebody supplies a
# picture per creep. It is the standing answer rather than a candidate here.
#
# NOTHING HERE DECIDES ANYTHING. Signing one is #285's.
