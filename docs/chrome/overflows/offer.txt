# A rung's name drawn past both edges of the offer. Issue #282, question 2.
#
# THE QUESTION. The offer is 208 units wide and a rung is one line of text:
# the name, three spaces, the price. On a capstone edge the price is the
# words "1 capstone token", so "Consecration   1 capstone token" is the
# longest label the surface ever carries, and it runs off both sides of the
# panel onto the board behind. Every capstone edge reads a token, so the run
# hits it at every line's top.
#
# THE STATE. Wave 9, a Cleric placed and climbed to a Bishop through
# ComposedRound.Do, and the Bishop's ladder opened -- which offers the
# Consecration for a token, because by wave 9 the run holds one. The rounds
# before it are played as wave-bar.txt says.
#
# THE CANDIDATES, each a class in client/Assets/Editor/ChromeCandidates/:
#
#   offer-as-built.png                 the ladder as it ships -- the baseline
#   offer-fits-its-longest-rung.png    OfferFitsItsLongestRung: width auto,
#                                      minimum 208; the panel sizes itself to
#                                      its longest rung. The option the layout
#                                      engine gives for free. What it costs is
#                                      centring: the offer is placed by its
#                                      left edge at half of 208, so a wider
#                                      panel hangs to the right of its hex.
#   offer-is-wider.png                 OfferIsWider: a fixed 336, pulled back
#                                      by half the difference so it stays
#                                      centred on the hex
#   offer-prices-beneath-the-name.png  OfferPricesBeneathTheName: each rung
#                                      two lines, the name at 18 over the
#                                      price at 15, the rung 64 tall
#
# NOT MOCKED, AND WHY: shorter words for the price. "1 token" would fit, and
# it is a word a player reads, which makes it Sam's to spell rather than a
# candidate an agent puts on a sheet. RosterNames.CapstoneToken() is where it
# would change.
#
# NOTHING HERE DECIDES ANYTHING. Signing one is #285's.
