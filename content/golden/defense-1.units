# The unit type table.
#
# ONE GLOBAL ID SPACE. Towers and creeps are the same kind of thing here, so
# an id is unique across every unit that has ever existed, is never reused,
# and is never an index into anything. A record that pins type 2 today still
# means this row in five years. The label beside it is for people reading this
# file and for error messages; nothing in the simulation branches on it, and
# renaming one does not move the content hash.
#
# INTEGERS ONLY, AND NO DECIMAL POINT. The parser refuses a '.' or a ',' on a
# data line before it even tokenises, so a designer typing 1.5 gets a load
# error rather than a float in the tick loop. Fractions are authored as the
# two integers they are made of, in units named by the column: thousandths of
# a hex, or ticks.
#
# FIELD ORDER IS WHAT THE CONTENT HASH FOLDS. Reindent a column, rewrap this
# comment or convert the line endings and the hash does not move, so no stored
# record is falsely retired. Change one number and it does.
#
# THE LAYOUT ROW BELOW SAYS WHICH COLUMNS THESE ARE. It comes before the first
# unit row because it is what says how to read one, and there is a reader
# branch per layout: a table written in the fifteen-column layout 1 still
# loads, folds under its own hash label and keeps the hash it always had, which
# is why the golden bundles pinned to one still replay. Adding or moving a
# column is a new layout and a new label, and a row whose field count does not
# match the layout it declared is refused rather than read against shifted
# fields.
#
# A NEW UNIT IS A ROW AND NEVER A COLUMN. Every shape below -- a swarm, a wall
# that walks, a sniper that outranges every other tower by half again, a sieger
# whose shell is nearly a second in the air -- is authored out of the columns
# that are already here. That is what the layout row costs: a column is a format
# version, a hash label and every record made under the old one retired, and a
# row is a line.
#
# Ids ascend strictly down the file, which is what makes a duplicate a
# comparison against the row above rather than a lookup nobody performed. They
# ascend past the roles, so a creep authored after a tower sits below it.
#
# EVERY DAMAGE AND HEALTH NUMBER IS AT THE TENFOLD SCALE. Multiplying both
# together leaves shots-to-kill where it was and buys resolution: armour and
# the type chart have ten times as many distinct results to land on across
# their range, which is what makes them tunable at all. It also widens the
# damage roll's range tenfold, so the seeded stream produces different numbers
# per shot and the golden trace beside these files is a different trace than
# the one the pre-scale numbers produced.
#
# EVERY WALKING ROW COSTS ITS EFFECTIVE HEALTH OVER A HUNDRED AND SIXTY, and
# every one of them lands within a few percent of that. A leak charges health
# equal to what the creep cost to send, one for one, so what a sauce buys is
# health a defense has to spend to stop it -- and effective health is the pool
# times the armour multiplier, which is the one thing a defense cannot argue
# with. The type chart deliberately stays out of the price: every row and every
# column of the matrix is a permutation of the same three cells, so averaged
# over the three attack types every armour type is worth exactly the same, and
# an armour type that changed the price would be charging twice for a bet.
#
# WHAT THAT LEAVES A ROW TO DIFFER IN IS SHAPE. Speed, granularity, and which
# defense chews it. A wisp column is fifty-seven bodies at seven sauce and a
# bulwark column is eight at forty-five, and against the committed defense they
# return the same fraction of the sauce they cost -- so choosing between them is
# a read of the board rather than a lookup of which row was mispriced.
#
# THESE NUMBERS ARE TUNED TO A PARTIAL BREAK. Against the committed defense and
# the committed wave, thirteen of forty creeps get through -- a third, near
# enough -- over sixty-one seconds. That is the point of the tuning and not a
# coincidence: a defense that holds tells you nothing when it changes, and a
# defense that collapses tells you nothing either. A third leaking means the
# leak count is a number a person can watch, and the golden trace beside these
# files is the run that produced it.
#
# THE COMMITTED DEFENSE IS FOUR BOLTS AND TWO MORTARS, so it is a pierce line
# with an impact tail, and every health pool below is authored against what that
# line actually deals rather than against a raw roll. Pierce takes 70 against
# Armoured and 140 against Swift, so armour for armour an Armoured body stands
# about a quarter longer in front of it than its number says and a Swift body
# about a sixth less. Send the same two into a magic line and the two trade
# places, which is the whole of what the matrix is for.
#
# THE GRUNT AND THE RUNNER ARE THE PAIR THE COMMITTED WAVE SENDS, and they
# differ in speed, maximum hp and armour type -- and in the cost that follows
# from those -- and in nothing else, so that a later fast group catches an
# earlier slow group and unit ordering stops being theoretical.
#
# THE RUNNER IS EXACTLY TWICE THE GRUNT'S SPEED, and that is load-bearing rather
# than tidy. Two creeps are level for exactly one tick as one passes the other,
# and being level is the case the target-selection tiebreak exists for -- pick
# the creep furthest along, and the lower id if two are equal. With speeds that
# were merely different, the two would step past each other without ever landing
# on the same distance, the tiebreak would never once be consulted, and a rule
# nothing exercises is a rule nobody has checked. Paired with the wave releasing
# a grunt column and a runner column on the same tick, this makes the pass a
# thing that happens repeatedly and visibly. The lancer walks at the grunt's
# speed exactly and the wisp at three times it, so those three meet on whole
# ticks too; the bulwark and the drifter deliberately do not, because a pass
# that lands between ticks is the ordinary case and it needs to exist as well.
#
#      id  label   role    maxHp  speed  range  cooldown  windup  backswing  dmgMin  dmgMax  delivery    flight  dying  cost  attack  armour    armourValue
#
# role     : moving (walks the corridor) | placed (stands where it was put)
# maxHp    : zero means the unit has no health pool and cannot be damaged
# speed    : thousandths of a hex per tick
# range    : thousandths of a hex
# flight   : ticks a projectile spends travelling; zero unless delivery is projectile
# dying    : ticks spent dying, so seeking into a death shows a death
# cost     : sauce, and the health a leak of this creep costs one for one
# attack   : pierce | impact | magic, and none for a unit that never attacks
# armour   : swift | armoured | arcane, and none for a unit with no health pool
# armourValue : percent of base effective health added per point; none carries zero

layout 2

unit   1   grunt    moving  1550   85     0      0         0       0          0       0       none        0       12     10    none    armoured   0
unit   2   runner   moving  1500   170    0      0         0       0          0       0       none        0       12      9    none    swift      0
unit   3   bolt     placed  0      0      3200   6         3       2          90      150     hitscan     0       0      40    pierce  none       0
unit   4   mortar   placed  0      0      4600   18        7       5          210     340     projectile  11      0      90    impact  none       0
unit   5   wisp     moving  1100   255    0      0         0       0          0       0       none        0       8       7    none    arcane     0
unit   6   bulwark  moving  5000   55     0      0         0       0          0       0       none        0       20     45    none    armoured  45
unit   7   drifter  moving  2400   100    0      0         0       0          0       0       none        0       12     19    none    arcane    25
unit   8   lancer   moving  3400   85     0      0         0       0          0       0       none        0       14     21    none    swift      0
unit   9   sniper   placed  0      0      8000   45        20      10         1300    1700    hitscan     0       0      200   magic   none       0
unit  10   sieger   placed  0      0      6500   60        25      12         2600    3400    projectile  25      0      300   impact  none       0
